export default function Line(props) {
    return (
        <div style={
            {
                width: props.width,
                height: props.height
            }
        }
        class='line'>
        </div>
    );
}