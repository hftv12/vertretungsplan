export default function Placeholder(props: any) {
    return (
        <div style={
            {
                width: props.width,
                height: props.height
            }
        }>
            
        </div>
    );
}