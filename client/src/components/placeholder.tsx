export default function Placeholder(props) {
    return (
        <div style={
            {
                width: props.width,
                height: props.height
            }
        }>
            
        </div>
    );
}